# Loaded for interactive shells
export HISTSIZE=1000000
export HISTFILESIZE=$HISTSIZE
export SAVEHIST=$HISTSIZE
readonly HISTFILESIZE SAVEHIST HISTSIZE 2>/dev/null

case "${BUILDENV-}" in
"") ;;
*)
        PS1="(buildenv) ${PS1}"
	;;
esac


source_local "${HOME}/.rc.common"
# vim: set filetype=sh:
